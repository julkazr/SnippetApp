import { EqualStringsDirective } from './equal-strings.directive';

describe('EqualStringsDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualStringsDirective();
    expect(directive).toBeTruthy();
  });
});
